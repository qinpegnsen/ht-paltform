import { StrToNumberPipe } from './str-to-number.pipe';

describe('StrToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new StrToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
