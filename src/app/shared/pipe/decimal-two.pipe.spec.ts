import { DecimalTwoPipe } from './decimal-two.pipe';

describe('DecimalTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
